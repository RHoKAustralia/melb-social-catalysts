{{!< default}}

{{! The comment above "< default" means - insert everything in this file into
    the {body} of the default.hbs template, which contains our header/footer. }}

{{! The big featured header on the homepage, with the site logo and description }}
{{> header}}

{{! The main content area on the homepage }}
<main class="content" role="main">
   
    {{! Each post will be output using this markup }}
    {{#foreach posts}}

        <article class="post-title {{post_class}}">
            <header class="post-header">
                {{#if image}} <a href="{{url}}"><img src="{{image}}"/></a> {{/if}}
                <h2 class="post-title"><a href="{{url}}">{{{title}}}</a></h2>
                <section class="post-meta">
                    {{author}} on <time class="post-date" datetime="{{date format='YYYY-MM-DD'}}">{{date format="DD MMMM YYYY"}}</time>
                </section>
            </header>
            <section class="post-excerpt">
                <p><a class="read-more" href="{{url}}">{{excerpt}}&hellip;</a></p>
            </section>
            {{#if tags}}
                <section style="border-top:1px solid #d3d3d3; font-size:12px; color:#999;">#Tags: 
                   {{#tags}}<li>{{name}}</li>{{/tags}}
                </section>
            {{/if}}
        </article>
    
    {{/foreach}}

    {{!! After all the posts, we have the previous/next pagination links }}
    {{pagination}}
   
</main>
